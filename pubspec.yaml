name: diadot
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  redux: ^5.0.0
  flutter_redux: ^0.8.2
  redux_thunk: ^0.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  fonts:
   - family: Inder
     fonts: 
      - asset: assets/Inder-Regular.ttf

  uses-material-design: true

